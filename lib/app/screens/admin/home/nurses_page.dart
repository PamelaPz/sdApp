import 'package:flutter/material.dart';

class NursesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Nurses Page'),
    );
  }
}
