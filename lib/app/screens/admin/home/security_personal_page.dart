import 'package:flutter/material.dart';

class SecurityPersonalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Personal Security Page'),
    );
  }
}
